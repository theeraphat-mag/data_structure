# 📚 Data Structure

รวมโค้ดโครงสร้างข้อมูล (Data Structure) ที่เขียนด้วยภาษา C ครอบคลุมเนื้อหาพื้นฐานสำคัญ ได้แก่ Stack, Queue, Linked List, Infix-Postfix, Binary Search Tree และ Graph

---

## 📁 โครงสร้างโปรเจกต์

```
data_structure/
├── stack/
│   ├── stack_static.c
│   └── stack_dynamic.c
├── queue/
│   ├── queue.c
│   └── queue_dynamic.c
├── link list/
│   ├── link_list.c
│   └── more_linklist.c
├── infix_postfix/
│   ├── in_pos_static.c
│   └── infixTopostfix.c
├── tree/
│   ├── binary_tree.c
│   └── ex_tree.c
├── UndirectedGraph/
│   └── graph.c
└── README.md
```

---

## 📦 รายละเอียดแต่ละโฟลเดอร์

### 1. Stack (`stack/`)

#### `stack_static.c` — Stack แบบ Static (Array)
- ใช้ Array ขนาดคงที่ (`MAX = 100`) ในการเก็บข้อมูล Stack
- ฟังก์ชันที่มี:
  - `push()` — เพิ่มข้อมูลเข้า Stack
  - `pop()` — นำข้อมูลบนสุดออกจาก Stack
  - `print_stack()` — แสดงข้อมูลทั้งหมดใน Stack
  - `destroy()` — ลบข้อมูลทั้งหมดใน Stack
- มีเมนูให้เลือกการทำงาน (Push, Pop, Print, Destroy, Exit)

#### `stack_dynamic.c` — Stack แบบ Dynamic (Linked List)
- ใช้โครงสร้าง Linked List ในการเก็บข้อมูล Stack (จัดสรรหน่วยความจำแบบ Dynamic)
- ฟังก์ชันที่มี:
  - `push()` — เพิ่มข้อมูลเข้า Stack
  - `pop()` — นำข้อมูลบนสุดออกจาก Stack พร้อม `free()` หน่วยความจำ
  - `stack_top()` — ดูค่าบนสุดของ Stack โดยไม่ลบออก
  - `print_stack()` — แสดงข้อมูลทั้งหมดใน Stack
  - `destroy()` — ลบข้อมูลทั้งหมดใน Stack พร้อมคืนหน่วยความจำ
- มีเมนูให้เลือกการทำงาน (Push, Pop, Print, Destroy, Top, Exit)

---

### 2. Queue (`queue/`)

#### `queue.c` — Queue แบบ Static (Array)
- ใช้ Array ขนาดคงที่ (`SIZE = 100`) ในการเก็บข้อมูล Queue (เก็บข้อมูลชนิด `char`)
- ฟังก์ชันที่มี:
  - `create_queue()` — สร้าง Queue เริ่มต้น
  - `enqueue()` — เพิ่มข้อมูลเข้าท้าย Queue
  - `dequeue()` — นำข้อมูลหน้า Queue ออก
  - `front_queue()` — ดูค่าหน้าสุดของ Queue
  - `destroy_queue()` — ลบข้อมูลทั้งหมดใน Queue
  - `print_queue()` — แสดงข้อมูลทั้งหมดใน Queue

#### `queue_dynamic.c` — Queue แบบ Dynamic (Linked List)
- ใช้โครงสร้าง Linked List ในการเก็บข้อมูล Queue (เก็บข้อมูลชนิด `char`)
- ฟังก์ชันที่มี:
  - `enqueue()` — เพิ่มข้อมูลเข้าท้าย Queue
  - `dequeue()` — นำข้อมูลหน้า Queue ออก พร้อม `free()` หน่วยความจำ
  - `front_queue()` — ดูค่าหน้าสุดของ Queue
  - `destroy()` — ลบข้อมูลทั้งหมดใน Queue พร้อมคืนหน่วยความจำ
  - `print_queue()` — แสดงข้อมูลทั้งหมดใน Queue

---

### 3. Linked List (`link list/`)

#### `link_list.c` — Linked List พื้นฐาน (Singly Linked List)
- ใช้โครงสร้าง Singly Linked List พร้อม pointer `head` และ `tail`
- ฟังก์ชันที่มี:
  - `append_list()` — เพิ่มข้อมูลต่อท้าย Linked List
  - `print_list()` — แสดงข้อมูลทั้งหมดใน Linked List
  - `seach_list()` — ค้นหาข้อมูลใน Linked List
  - `delete_node()` — ลบ Node ที่ระบุ (รองรับการลบ head, tail และ Node ตรงกลาง)
  - `destroy_list()` — ลบข้อมูลทั้งหมดใน Linked List
- มีเมนูให้เลือกการทำงาน (Add, Print, Search, Delete, Destroy, Exit)

#### `more_linklist.c` — Linked List แบบแทรกตำแหน่งต่าง ๆ
- สาธิตการแทรกข้อมูลในตำแหน่งที่หลากหลายของ Linked List
- ฟังก์ชันที่มี:
  - `append_front()` — เพิ่มข้อมูลข้างหน้า (แทรกหน้า head)
  - `append_list()` — เพิ่มข้อมูลต่อท้าย (แทรกหลัง tail)
  - `append_behindHead()` — เพิ่มข้อมูลต่อท้ายจาก head (ตำแหน่งที่ 2)
  - `append_beforTail()` — เพิ่มข้อมูลก่อนหน้า tail
  - `append_beforThreeTail()` — เพิ่มข้อมูลก่อนหน้า tail 3 ตำแหน่ง
  - `print_list()` — แสดงข้อมูลทั้งหมดใน Linked List

---

### 4. Infix / Postfix (`infix_postfix/`)

#### `in_pos_static.c` — แปลง Infix เป็น Postfix และคำนวณผลลัพธ์ (Array-based)
- ใช้ Array Stack ขนาดคงที่ (`MAX = 50`)
- รับนิพจน์ Infix จากผู้ใช้ แล้วแปลงเป็น Postfix
- คำนวณผลลัพธ์จากนิพจน์ Postfix
- ฟังก์ชันที่มี:
  - `push()` / `pop()` — จัดการ Stack
  - `priority()` — ตรวจสอบลำดับความสำคัญของ Operator (`^` > `*,/` > `+,-`)
  - `calculate()` — คำนวณ `+`, `-`, `*`, `/`, `^`
  - `infix_to_postfix()` — แปลงนิพจน์ Infix → Postfix
  - `process()` — คำนวณผลลัพธ์จาก Postfix (รองรับตัวเลขหลายหลัก)

#### `infixTopostfix.c` — แปลง Infix เป็น Postfix และคำนวณผลลัพธ์ (Linked List-based)
- ใช้ Linked List Stack แบบ Dynamic
- เก็บผลลัพธ์ Postfix ไว้ใน Linked List
- แสดงตารางขั้นตอนการแปลง (Step-by-step table) ทั้ง Infix → Postfix และ Postfix → ผลลัพธ์
- ฟังก์ชันที่มี:
  - `push()` / `pop()` — จัดการ Stack (Linked List)
  - `append_postfix()` — เก็บผลลัพธ์ Postfix ใน Linked List
  - `priority()` — ตรวจสอบลำดับความสำคัญของ Operator
  - `calculate()` — คำนวณ `+`, `-`, `*`, `/`, `^`
  - `infix_to_postfix()` — แปลงนิพจน์ Infix → Postfix พร้อมแสดงตาราง
  - `process_expressions()` — คำนวณผลลัพธ์จาก Postfix พร้อมแสดงตาราง
  - `print_step_inTopost()` / `print_step_postToin()` — แสดงขั้นตอนในรูปแบบตาราง

---

### 5. Tree (`tree/`)

#### `binary_tree.c` — Binary Search Tree (BST) พร้อม Destroy
- โครงสร้าง Binary Search Tree พร้อมฟังก์ชันครบชุด
- ฟังก์ชันที่มี:
  - `add()` — เพิ่ม Node ใน BST (ค่าน้อยไปซ้าย ค่ามากไปขวา, ตรวจสอบค่าซ้ำ)
  - `pre_traverse()` — แสดงผล Pre-order (Root → Left → Right)
  - `in_traverse()` — แสดงผล In-order (Left → Root → Right)
  - `post_traverse()` — แสดงผล Post-order (Left → Right → Root)
  - `search()` — ค้นหาข้อมูลใน BST
  - `destroy_tree()` — ลบ Tree ทั้งหมด พร้อมคืนหน่วยความจำ (Recursive)
- มีเมนูให้เลือกการทำงาน (Add, Pre-order, In-order, Post-order, Destroy, Exit)

#### `ex_tree.c` — Binary Search Tree (BST) แบบพื้นฐาน
- โครงสร้าง Binary Search Tree เหมือน `binary_tree.c` แต่ไม่มีฟังก์ชัน `destroy_tree()`
- ฟังก์ชันที่มี:
  - `add()` — เพิ่ม Node ใน BST
  - `pre_traverse()` — แสดงผล Pre-order
  - `in_traverse()` — แสดงผล In-order
  - `post_traverse()` — แสดงผล Post-order
  - `search()` — ค้นหาข้อมูลใน BST
- มีเมนูให้เลือกการทำงาน (Add, Pre-order, In-order, Post-order, Exit)

---

### 6. Undirected Graph (`UndirectedGraph/`)

#### `graph.c` — กราฟไม่มีทิศทาง (Adjacency Matrix) พร้อม Euler Circuit
- ใช้ Adjacency Matrix ขนาด `100 x 100` ในการเก็บกราฟ
- ฟังก์ชันที่มี:
  - `addEdge()` — เพิ่ม Edge เชื่อมระหว่าง 2 Vertex (ทั้งขาไปและขากลับ)
  - `setGraph()` — กำหนดค่าเริ่มต้นของกราฟ (set 0 ทั้งหมด)
  - `printGraph()` — แสดง Adjacency Matrix ของกราฟ
  - `dfs()` — Depth-First Search สำหรับตรวจสอบการเชื่อมต่อ
  - `isConnected()` — ตรวจสอบว่ากราฟเป็นกราฟเชื่อมต่อ (Connected Graph) หรือไม่
  - `isEuler()` — ตรวจสอบว่ากราฟเป็นกราฟ Euler หรือไม่ (Vertex ทุกตัวต้องมี Degree เป็นจำนวนคู่)
  - `eulerCircuit()` — หาวงจร Euler (Hierholzer's Algorithm)
  - `subCircuit()` — หาวงจรย่อย (Sub-circuit) ในกราฟ
- ขั้นตอนการทำงาน: ตรวจสอบ Connected → ตรวจสอบ Euler → หาวงจรย่อย → หาวงจร Euler

---

## 🛠️ วิธีคอมไพล์และรัน

ใช้ GCC compiler:

```bash
gcc <ชื่อไฟล์>.c -o <ชื่อโปรแกรม>
./<ชื่อโปรแกรม>
```

ตัวอย่าง:

```bash
gcc stack/stack_static.c -o stack_static
./stack_static
```

---

## 📝 ภาษาที่ใช้

- **C** (ISO C99)
